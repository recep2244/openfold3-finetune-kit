## What this changes
Briefly describe the change and why.

## How it was tested
- [ ] `bash -n` on changed scripts
- [ ] `shellcheck` clean (or warnings only)
- [ ] `yamllint` on changed configs
- [ ] notebooks validate / outputs cleared
- [ ] ran `scripts/verify_setup.sh` (if GPU available)

## Checklist
- [ ] No weights, datasets, or run outputs committed
- [ ] Docs updated if behavior changed
